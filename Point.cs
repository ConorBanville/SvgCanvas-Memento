namespace SvgCanvas
{
    public class Point
    {
        public float X {get;}   // Store the x position of the point 
        public float Y {get;}   // Store the y position of the point

        // Struct Constructor
        public Point(float x, float y)
        {
            X = x;
            Y = y;
        }
    }
}